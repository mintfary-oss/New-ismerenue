# СТАТУС ПРОЕКТА
## Что уже сделано и на каком пункте остановились

---

## ЧТО УЖЕ СДЕЛАНО

### Шаги 1–5 — Анализ ТЗ и документация ✅
- Прочитаны все документы закупки №0339300000126000515
- Созданы: план реализации, аналитические заметки, исследование аналогов, архитектура

### Шаг 6 — Исследование 17 open-source проектов ✅
Magistrala, CasaOS, OpenAQ, EnviroMonitor, Prometheus, Telegraf, aqi_watcher + 10 других.
Проанализированы 22 ошибки, выбран технологический стек.

### Шаг 7 — SPRINT 1: Написан Go-код (39 файлов) ✅
**Коммит: `ac555db` | 3787 строк кода**

| Файл | Описание |
|---|---|
| `cmd/aqi-platform/main.go` | Точка входа, cobra CLI |
| `internal/config/config.go` | Все структуры конфигурации (server, db, redis, auth, email, forecast) |
| `internal/config/loader.go` | Viper loader: ENV + yaml + defaults |
| `internal/domain/user.go` | User entity + роли (admin/analyst/viewer) |
| `internal/domain/sensor.go` | Sensor entity (gas/dust/combo) |
| `internal/domain/measurement.go` | Measurement entity + filters |
| `internal/domain/aqi.go` | AQI расчёт (US EPA breakpoints) + российская категоризация |
| `internal/domain/errors.go` | Domain errors + AppError с HTTP кодами |
| `internal/server/server.go` | HTTP сервер, graceful shutdown 30 сек |
| `internal/server/router.go` | Chi роутер, 40+ маршрутов |
| `internal/handler/handler.go` | Deps container + writeJSON/writeError helpers |
| `internal/handler/health.go` | GET /health + GET /ready (с проверкой БД) |
| `internal/handler/stubs.go` | Заглушки всех handlers (501 Not Implemented) |
| `internal/middleware/auth.go` | JWT validation + OptionalAuth + APITokenAuth |
| `internal/middleware/rbac.go` | RequireRole/RequireAdmin/RequireAnalystOrAdmin |
| `internal/middleware/security.go` | Security headers OWASP + Widget CSP |
| `internal/middleware/ratelimit.go` | Token bucket: Login(10/15min), API(100/min), Widget(300/min) |
| `internal/repository/postgres.go` | pgx/v5 пул соединений |
| `internal/repository/redis.go` | Redis client + TokenBlacklist + LoginAttemptTracker |
| `internal/repository/queries/*.sql` | 5 SQL файлов: users, sensors, measurements, tokens, audit |
| `internal/service/auth_service.go` | Argon2id(OWASP 2025) + JWT access(15m)/refresh(30d) + rotation |
| `migrations/000001_init_schema.up.sql` | 9 таблиц: users, sensors, measurements, forecasts, api_tokens, audit_log, uploaded_data, feedback |
| `migrations/000002_timescaledb.up.sql` | Hypertables + Continuous Aggregate (1h) + Retention 60 мес |
| `docker/Dockerfile` | Multi-stage build → 2.5 МБ бинарник (distroless/static) |
| `docker/docker-compose.yml` | 4 сервиса: app + postgres(TimescaleDB) + redis + nginx |
| `docker/nginx/nginx.conf` | Reverse proxy + TLS 1.2/1.3 + HSTS + JSON логи |
| `scripts/install.sh` | Автоустановщик: проверка Docker → генерация секретов → запуск |
| `Makefile` | build, test, lint, docker, up, down, migrate, sqlc |
| `.env.example` | Пример переменных окружения |
| `sqlc.yaml` | Конфигурация кодогенератора sqlc |

**Проверки:**
- `go build ./...` ✅
- `go vet ./...` ✅
- Размер бинарника: 2.5 МБ (статически слинкованный, без CGO)
- Язык: Go 1.26 (установлен на сервере)

### Git-коммиты ✅
| Хеш | Что добавлено |
|---|---|
| `09d9e92` | `ПЛАН_РЕАЛИЗАЦИИ_ПРОЕКТА.md` + `АНАЛИТИЧЕСКИЕ_ЗАМЕТКИ.md` |
| `3a4adfb` | `ПЕРЕПИСКА_С_NEO.md` + `СТАТУС_ПРОЕКТА.md` |
| `49c1e09` | Обновление: сообщение 3 |
| `1580258` | `ИССЛЕДОВАНИЕ_АНАЛОГОВ.md` + `АРХИТЕКТУРА_GO_ПРИЛОЖЕНИЯ.md` |
| `64a3bc2` | Обновление: сообщение 5 |
| `ac555db` | **Sprint 1: 39 файлов Go-кода** |
| текущий | Обновление: переписка + статус |

---

## НА КАКОМ ПУНКТЕ ОСТАНОВИЛИСЬ

**Текущий статус:** Sprint 1 завершён. Фундамент приложения написан и компилируется.

### Что ГОТОВО:
- [x] Фундамент Go-проекта (структура, зависимости, main.go)
- [x] Конфигурация (Viper + ENV + defaults)
- [x] Доменные модели (user, sensor, measurement, aqi, errors)
- [x] HTTP сервер (Chi v5 + graceful shutdown)
- [x] 40+ маршрутов (заглушки → будут реализованы в Sprint 2–7)
- [x] Middleware (auth, RBAC, security OWASP, rate limit)
- [x] Database layer (pgx/v5 + Redis + TokenBlacklist + SQL запросы)
- [x] Auth service (Argon2id + JWT access/refresh + token rotation)
- [x] SQL миграции (9 таблиц + TimescaleDB hypertables)
- [x] Docker (Dockerfile multi-stage + docker-compose.yml + nginx.conf)
- [x] install.sh (автоустановщик)
- [x] Makefile (все команды разработки)

### Шаг 8 — SPRINT 2: Реальная бизнес-логика ✅
**Коммит: Sprint 2 | 30 Go-файлов, 4407 строк**

| Новый файл | Что реализовано |
|---|---|
| `internal/repository/user.go` | UserRepo: GetByEmail, GetByID, List, Create, Update, Delete, Count; APITokenRepo |
| `internal/repository/sensor.go` | SensorRepo: List, GetByID, Create, Update, Delete, UpdateLastSeen |
| `internal/repository/measurement.go` | MeasurementRepo: Insert, InsertBatch, List, Aggregate (time_bucket), Latest, LatestBySensor, DeleteOlderThan |
| `internal/repository/postgres.go` | RunMigrations → реальная реализация через golang-migrate v4 |
| `internal/service/user_service.go` | UserService: CRUD + хэширование пароля + проверка уникальности email |
| `internal/service/sensor_service.go` | SensorService: CRUD + SensorStatus (online/offline) |
| `internal/service/measurement_service.go` | MeasurementService: Ingest (one + batch) + List/Aggregate/Latest |
| `internal/handler/auth_handler.go` | AuthHandler: Login, Refresh, Logout (реальная реализация) |
| `internal/handler/user_handler.go` | UserHandler: List, Create, Get, Update, Delete |
| `internal/handler/sensor_handler.go` | SensorHandler: List, Create, Get, Update, Delete, Status |
| `internal/handler/measurement_handler.go` | MeasurementHandler: List, Latest, Aggregate; IngestHandler: Upload |
| `internal/handler/handler.go` | Deps обновлён: AuthSvc, UserSvc, SensorSvc, MeasureSvc |
| `internal/handler/stubs.go` | Оставлены только Forecast/Token/Report/Stats/Feedback/Widget (Sprint 3-4) |
| `cmd/aqi-platform/main.go` | runServer: полная DI-цепочка (pool→repo→service→handler→router→server) |
| `internal/domain/user.go` | Добавлен APIToken, CreateAPITokenInput |
| `internal/domain/sensor.go` | Добавлен SensorStatusResponse |

**Проверки:**
- `go build ./...` ✅
- `go vet ./...` ✅
- Размер бинарника: 14 МБ (включает golang-migrate + pgx5 driver)

### Следующий шаг — SPRINT 3 (по команде):
- [ ] Прогнозный модуль (EWMA + IDW интерполяция)
- [ ] `/api/v1/forecast/*` handlers
- [ ] Публичный виджет (HTML + JS, embed.FS)
- [ ] Планировщик (retention policy, pересчёт прогноза каждые 20 мин)
- [ ] Middleware Auth подключить к защищённым маршрутам

---

## ФАЙЛЫ В РЕПОЗИТОРИИ

| Файл/Директория | Статус | Описание |
|---|---|---|
| `ПЛАН_РЕАЛИЗАЦИИ_ПРОЕКТА.md` | ✅ | Бизнес-план по контракту |
| `АНАЛИТИЧЕСКИЕ_ЗАМЕТКИ.md` | ✅ | Разбор ТЗ |
| `ИССЛЕДОВАНИЕ_АНАЛОГОВ.md` | ✅ | Анализ 17 проектов |
| `АРХИТЕКТУРА_GO_ПРИЛОЖЕНИЯ.md` | ✅ | Полное ТЗ на разработку |
| `ПЕРЕПИСКА_С_NEO.md` | ✅ | История диалога |
| `СТАТУС_ПРОЕКТА.md` | ✅ | Этот файл |
| `aqi-platform/` | ✅ | **Исходный код Go-приложения (Sprint 1)** |

---

*Последнее обновление: август 2026, Sprint 1 завершён | Репозиторий: mintfary-oss/New-ismerenue*
